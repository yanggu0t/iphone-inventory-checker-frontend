import { useLocation } from "@tanstack/react-router";

const Models = () => {
  const search = useLocation().search;

  //   console.log(search, 121121);

  return <div>Models</div>;
};

export default Models;
